# 알려진 한계

`hwpctl`은 사용자가 열어 둔 한글 2022 문서를 직접 다루는 초기 편집 엔진입니다.
중요한 원본에는 먼저 복사본으로 다음 흐름을 확인하세요.

```text
복사본 열기 → status → snapshot → 작은 변경 1회 → 화면 확인 → Undo 확인 → save_as
```

## 열린 창과 대상 고정

- 연결은 Windows COM 실행 객체와 `pyhwpx`가 노출하는 한글 인스턴스에 의존합니다.
  한글 설치 종류, 보안 모듈, 권한 구성이 다르면 연결 결과도 달라질 수 있습니다.
- `open`으로 얻은 창 핸들을 기록하고 후속 명령에서 같은 창인지 확인하지만, 여러 창을
  동시에 사용하는 모든 조합을 보장하지는 않습니다. 한글을 재시작했거나 대상 불일치
  오류가 나면 원하는 창을 활성화한 뒤 인자 없이 `open`을 실행해 다시 고정하세요.
  인자 없는 `open`은 문서를 만들지 않으며, 빈 문서는 `open --new`로만 만듭니다.
- 자동 테스트는 COM 스텁을 포함하며 실제 한글 UI 검증을 대체하지 않습니다. 공식 릴리스
  전에 [수동 체크리스트](docs/RELEASE_CHECKLIST.md)를 별도로 통과해야 합니다.

## Undo와 수동 편집

- `undo`는 hwpctl이 실행한 명령의 예상 Undo 단계 수를 기록합니다. 한글 자동화에는
  사용자의 수동 편집과 hwpctl 편집을 신뢰성 있게 구분하는 공개 리비전 API가 없습니다.
- hwpctl 명령 사이에 사용자가 직접 편집하면 `hwpctl undo`가 그 수동 편집부터 되돌릴 수
  있습니다. 이 경우 hwpctl의 `undo` 대신 한글 화면에서 결과를 확인하며 Ctrl+Z를
  사용하세요.
- `fill_cells`, 글자·문단·셀 서식의 실제 Undo 묶음은 문서 상태나 한글 빌드에 따라 달라질
  수 있으므로 처음 사용하는 작업은 복사본에서 확인해야 합니다.

## 표·쪽·차트와 서식

- `snapshot`은 캐럿과 블록 선택을 복원하려고 시도하지만 특수 개체나 손상된 표가 있는
  문서에서는 복원에 실패할 수 있습니다. 표 미리보기는 최대 10개, 표당 8행×8열입니다.
- 내부 가로 테두리 `TypeHorz`는 한글 2022에서 지원되지 않아 명시적으로 거부합니다.
- `insert_chart`의 `line` 차트 인덱스는 추론값이며 `ChartDataDialogDisable` 동작도 실기
  확인 대상입니다. 데이터 편집 대화상자가 뜨면 자동으로 누르지 말고 중단하세요.
- 현재 `set_format`은 글꼴·크기·굵게·기울임·글자색·정렬·셀 채우기·글자 그림자를 지원합니다.
  줄 간격은 레이아웃 검토에서 읽지만 직접 변경 도구로는 아직 노출하지 않습니다.
- `set_table_position`의 floating 좌표는 표가 놓인 문단/쪽/용지를 기준으로 계산됩니다.
  앞 문단의 길이나 쪽 나눔이 바뀌면 절대 화면 좌표처럼 고정되지 않을 수 있으므로,
  문서 조립이 끝난 뒤 PDF로 위치와 본문 흐름을 확인하세요.
- `set_page_visibility`와 `restart_page_number`는 현재 캐럿 위치에 한/글 네이티브
  제어를 삽입합니다. 구역·쪽 나눔을 다시 구성했다면 표지/다음 쪽의 번호와 숨김 상태를
  PDF에서 함께 확인하세요.
- `insert_text_box`의 단색/선형 그라데이션, 도형 그림자, 글자 그림자는 한글 2022에서
  실기 검증했습니다. 다만 현재 검증한 설치본은 글상자 내부 여백을 쓰는
  `DrawTextBox` COM 항목을 노출하지 않아, `margin`을 지정하면 조용히 기본값으로
  처리하지 않고 명시적으로 실패합니다. 표 기반 레이아웃은 `set_cell_margin`을
  사용하세요.
- 변경 추적, 암호화 문서, 매크로, 복잡한 개체, 모든 HWP/HWPX 변환을 지원하는 범용
  문서 처리기는 아닙니다.
- `hwpx_status` / `hwpx_inspect` 는 한글 없이 `.hwpx` 서식 그룹만 읽습니다.
  공고문 재현·쪽 이미지 비교·HWPX 쓰기 CLI 는 아직 없습니다.
  단계는 [HWPX 업그레이드 계획](docs/hwpx-upgrade-plan.md)을 따릅니다.

## 저장과 데이터 보호

- 자동 저장하지 않습니다. 기본 저장 방식은 새 경로의 `save_as`입니다. 원본 덮어쓰기는
  `save --overwrite`, 현재 문서 닫기는 `close --force`를 명시해야 합니다. 모든 열린
  문서를 닫는 `close_all --force`는 다른 저장되지 않은 문서도 버리므로 사용자가 그
  범위를 명시적으로 요청한 경우에만 사용하세요.
- 연결된 MCP 클라이언트는 문서 내용을 읽고 편집할 수 있습니다. 신뢰하는 로컬
  클라이언트와 계정에서만 사용하고, HTTP 연결은 [보안 정책](SECURITY.md)을 따르세요.

## 지원 범위와 비제휴 고지

- 목표 환경은 Windows와 한글 오피스 2022이며, 현재 문서화된 실측 기준은 한글
  `12.0.0.850`과 `pyhwpx 1.7.2`입니다. 다른 빌드와 한글 버전은 보장하지 않습니다.
- `hwpctl`은 한글과컴퓨터가 제공·보증·후원하는 공식 제품이 아닙니다. 사용자는 유효한
  제품 라이선스와 자동화 사용 조건을 직접 확인해야 합니다.

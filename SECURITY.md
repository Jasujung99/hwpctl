# 보안 정책

## 신뢰 경계

`hwpctl`은 사용자의 Windows 계정 권한으로 열린 한글 문서를 읽고 편집합니다. MCP 서버에
연결하는 AI 클라이언트에는 사실상 해당 문서의 읽기·편집 권한이 부여됩니다. 출처를
신뢰할 수 없는 클라이언트, 확장 프로그램, 프롬프트에는 연결하지 마세요.

`hwpctl` 자체는 문서 내용을 외부 서비스에 보내도록 설계되지 않았습니다. 다만 Codex,
Claude Code, Cursor, Gemini CLI, Grok Build 등 연결한 클라이언트의 데이터 처리 정책은
별개입니다. 민감한 문서를 읽히기 전에 해당 클라이언트의 설정과 정책을 확인하세요.

## 연결 방식

- 기본 권장 방식은 로컬 stdio MCP이며 네트워크 포트를 열지 않습니다.
- HTTP 모드는 loopback 주소와 토큰을 요구합니다. 토큰은 환경 변수나 안전한 비밀 저장소로
  전달하고 저장소·로그·스크린샷에 남기지 마세요.
- 터널이나 포트 전달로 HTTP 서버를 외부에 연결하면 토큰을 가진 외부 주체에게 열린
  문서의 읽기·편집 권한을 줄 수 있습니다. 저장소는 외부 공개를 기본값으로 제공하지
  않습니다.

## 취약점 보고

보안 문제는 공개 Issue로 올리지 마세요. 저장소의 **Security → Report a vulnerability**를
사용해 GitHub Private Vulnerability Reporting으로 알려 주세요.

보고에는 민감정보를 제거한 다음 항목만 포함해 주세요.

- 영향을 받는 hwpctl, Windows, 한글, Python, `pyhwpx` 버전
- 개인정보 없는 최소 재현 절차와 예상·실제 결과
- 문서 노출, 임의 편집, 인증 우회 가능성

실제 문서, 토큰, 쿠키, 사용자명, 컴퓨터명, 절대 경로는 첨부하지 마세요. 비공개 신고
기능을 사용할 수 없다면 저장소 소유자에게 GitHub 프로필을 통해 안전한 연락 경로를 먼저
요청하고, 민감한 세부정보를 공개하지 마세요.

## 지원 버전

첫 공개 릴리스 전에는 `main`의 최신 커밋만 보안 수정 대상입니다. `v0.1.0` 공개 후에는
최신 `0.1.x` 릴리스에 우선 반영합니다.
